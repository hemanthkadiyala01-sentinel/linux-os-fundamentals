# Validation Tests

Run `scripts/validation/validate-repository.ps1` from PowerShell. It verifies
the required evidence-first documentation exists and that generated evidence is
excluded from version control. The check does not claim to test Wazuh delivery;
that requires authorized manager and endpoint-log access.
